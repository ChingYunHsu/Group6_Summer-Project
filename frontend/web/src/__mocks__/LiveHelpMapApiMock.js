export const listVenues = jest.fn(async () => []);

export const getVenueById = jest.fn(async () => ({}));

export const getVenueBusyness = jest.fn(
  async () => ({})
);

export const getVenueBusynessForecast = jest.fn(
  async () => ({})
);

export const listReports = jest.fn(async () => []);