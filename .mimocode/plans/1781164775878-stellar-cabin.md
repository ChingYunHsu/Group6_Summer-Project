# 项目架构检查报告 vs project-status.md

## 总结

project-status.md 整体准确，但存在 **3 个实际 Schema 同步问题** 和若干文档不完整之处。

---

## 一、Schema 同步问题（关键）

### 1. Docker `venues` 表缺少 `district` 和 `open_now` 列

| 位置 | `district` | `open_now` |
|------|-----------|------------|
| `docker/mysql/init/001_clearpath_schema.sql` (L16-50) | ❌ 缺失 | ❌ 缺失 |
| `Data+ML/test/6.2-6.5_DB/001_clearpath_schema.sql` (L232-262) | ✅ `varchar(32)` | ✅ `tinyint(1)` |

**后果**: Docker schema 在 L379-380 有 `CREATE INDEX idx_venues_district ON venues(district)` —— 该索引引用不存在的列，**Docker 初始化时会报 SQL 错误**。

project-status.md L107 已记录 `district` 未同步，但 `open_now` 未提及。

### 2. `busyness_scores.forecast_1h` 类型不一致

| 位置 | 类型 |
|------|------|
| Docker schema (L211) | `INT` |
| Data+ML schema (L7) | `JSON` |
| project-status.md (L86) | 声称已改为 JSON ✅ 2026-06-09 |

**Docker schema 仍为 INT，未同步项目状态文档中的修改。**

### 3. `user_reports` Data+ML schema 仍含 `reported_by`

| 位置 | `reported_by` |
|------|--------------|
| Docker schema (L154-180) | ❌ 已移除，用 `user_id FK` 替代 ✅ |
| Data+ML schema (L164) | ⚠️ 仍存在 `reported_by varchar(50)` |

project-status.md L84 声称已修改 ✅ 2026-06-09，但 Data+ML schema 未同步。

---

## 二、文档不完整之处（非阻塞）

### 架构图 Blueprint 数量
- project-status.md L26 声称 **"9 blueprints"**
- 实际 `app.py` 注册 **8 个 blueprints**: health, integrations, user, routes, app_state, venues, reports, insights

### Mobile 前端未记录的文件
- `hooks/` 目录: `use-color-scheme.ts`, `use-color-scheme.web.ts`, `use-theme.ts`
- `types/` 目录: `medical.ts`, `profile.ts`
- `src/global.css`

### Web 前端未记录的文件
- `main.jsx`, `App.css`, `index.css`, `vite.config.js`, `index.html`, `assets/`

### 多个 Schema 文件未同步
仓库内有 4 份 schema，互相不一致：
1. `docker/mysql/init/001_clearpath_schema.sql` — 380 行，19 表
2. `Data+ML/test/6.2-6.5_DB/001_clearpath_schema.sql` — 263 行，15 表
3. `Data+ML/test/6.2_DB/001_clearpath_schema.sql` — 旧版本
4. `Data+ML/plan/6.2_CC/001_clearpath_schema.sql` — 计划版本

---

## 三、建议修复方案

### 优先级 P0 — Docker Schema 修复

**文件**: `docker/mysql/init/001_clearpath_schema.sql`

1. 在 venues CREATE TABLE 中添加:
   ```sql
   `district` VARCHAR(32),
   `open_now` BOOLEAN DEFAULT TRUE,
   ```
2. 将 `busyness_scores.forecast_1h` 从 `INT` 改为 `JSON`

### 优先级 P1 — Data+ML Schema 同步

**文件**: `Data+ML/test/6.2-6.5_DB/001_clearpath_schema.sql`

1. 添加 `users`, `user_favorite_venues`, `notification_preferences`, `report_categories`, `busyness_forecasts`, `venue_embeddings` 6 张缺失表
2. 在 `user_reports` 中移除 `reported_by` 列，添加 `user_id VARCHAR(36) NOT NULL`

### 优先级 P2 — project-status.md 文档修正

1. 架构图 "9 blueprints" → "8 blueprints"
2. 在 venues 表 24 列列表中添加 `district` 和 `open_now`（如果它们被添加到 Docker schema）
3. 添加 `hooks/`、`types/` 到 mobile 前端文件结构

---

## 四、验证步骤

修复后应执行：
```bash
# 1. 语法验证
docker compose up -d mysql && sleep 5
docker compose exec mysql mysql -u root -proot -e "SOURCE /docker-entrypoint-initdb.d/001_clearpath_schema.sql;"

# 2. 幂等性验证（再执行一次应无错误）
docker compose exec mysql mysql -u root -proot -e "SOURCE /docker-entrypoint-initdb.d/001_clearpath_schema.sql;"

# 3. Data+ML schema 语法验证
python -c "import ast; [ast.parse(open(f).read()) for f in __import__('glob').glob('Data+ML/**/*.py', recursive=True)]"
```
